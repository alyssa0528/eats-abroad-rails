<!-- form to be rendered when chef edits account info -->
<h1>Edit your Eats Abroad profile</h1>
<%= form_for(@chef) do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Update Account Info", class: ["button"] %>
<% end %>
